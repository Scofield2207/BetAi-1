{
  "name": "betai-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cyarn install && yarn build",
    "start": "vite start",
    "install-deps": "yarn install"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
